#include <string>
#include "Struct.h"

void free_memory()
{
    members.clear();
}



vector<man> members;

int main()
{
    int i = 1;
    string s = "";
    do
    {
        bool ext = false;
        cout << ">"; sin >> s
        switch (s)
        case "!add": ;// func add;
        case "!del": ;// func del;
        case "!chg": ;// func chg;
        case "!prt": ;// func prt;
        case "!frd": ;// func frd;
        case "!fwr": ;// func fwr;
        case "!hpl": ;// func hlp;
        case "!ext": ext = true;// func ext;
        default cout << "Wrong argement" << endl;
    }
    while(ext);
    free_memory;
    return 0;
}